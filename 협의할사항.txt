1. counting 정렬은 주어진 리스트에서 값을 옮기는 개념보다 새로운 리스트에다가 다시 채워넣는 개념이 강함.
counting_sort_1 은 값 옮기기 개념으로 drawList 구성
counting_sort_2 는 새 리스트에 채워넣기 개념으로 drawList2 구성  // 색 구분 적용이 제대로 되는지 미확인

둘 중 어느 방식으로 보여줄지 결정.

2. bogo 정렬은 색깔 구분이 필요 없어서 제외함.

3. bogo 선택시 웬만하면 프로그램이 안끝남. 랜덤으로 섞어서 운좋게 정렬되면 끝나는 알고리즘이라서.
   그래서 최대 시도 횟수를 정함. MAX = 리스트 길이 + 20번

4. intro 정렬은 quick, heap, insert 를 정렬 중간에 효율에 따라 골라쓰는 알고리즘.
   quick, heap, insert를 중복해서 구현함.
   코드라인을 줄이기 위해서 합칠 필요가 있으나 아래 5번 이유 때문에 힘들 수 있음.

5. intro 정렬에서는 여러 함수를 돌아다니며 drawList를 갱신해야하므로
   self.intro_drawList 라는 클래스 변수를 만들어서 관리하였음.
